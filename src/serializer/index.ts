import * as Full from "./full/full";

export const Serializer = {
  /**
   * Serializer for full schema which preserves full information and configuration.
   * Does not strip away any information and does not compress/reduce stored data in any way.
   */
  Full,
};
